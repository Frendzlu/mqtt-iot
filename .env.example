# MQTT IoT Dashboard - Environment Configuration
# Copy this file to .env and customize the values below
# Required: Docker Compose expects a .env file in the project root

# ============================================================================
# Mosquitto MQTT Broker Configuration
# ============================================================================
# Username for the administrative MQTT user
# This user can publish/subscribe to any topic and is used by the backend
MOSQ_USER=adm

# Password for the administrative MQTT user
# Change this to a secure password in production
MOSQ_PASS=sdf

# ============================================================================
# PostgreSQL Database Configuration
# ============================================================================
# PostgreSQL host (use 'postgres' for Docker service name)
DB_HOST=postgres

# PostgreSQL port (default: 5432)
DB_PORT=5432

# PostgreSQL username
DB_USER=mqtt_user

# PostgreSQL password
# Change this to a secure password in production
DB_PASSWORD=mqtt_pass

# PostgreSQL database name
DB_NAME=mqtt_db

# ============================================================================
# Backend Service Configuration
# ============================================================================
# MQTT broker username used by the backend service
# Should match MOSQ_USER above
BROKER_USER=backend-manager

# MQTT broker password used by the backend service
# Should match MOSQ_PASS above or be a dedicated backend user password
BROKER_PASS=supersecret

# Backend HTTP server port
PORT=3001

# ============================================================================
# Frontend Configuration
# ============================================================================
# Backend API URL for the frontend to connect to
# Update this if deploying to production or using different ports
VITE_BACKEND_URL=http://localhost:3001

# ============================================================================
# Optional: Custom Paths (Advanced Configuration)
# ============================================================================
# Uncomment and customize these paths if needed

# Path to users.json file (generated by backend)
# USERS_FILE=/mosquitto/data/users.json

# Path to Mosquitto password file (generated by backend)
# MOSQUITTO_PASSWD=/mosquitto/config/passwd

# Path to Mosquitto ACL file (generated by backend)
# MOSQUITTO_ACL=/mosquitto/config/acl

# Directory for storing uploaded images
# IMAGES_DIR=/app/images
